# Disable Jekyll processing - we're using React/Vite
# This file is kept for GitHub Pages compatibility only

# Exclude everything from Jekyll processing
exclude:
  - '*'
  - '.*'

# Keep only essential files
include:
  - '.nojekyll'
  - 'docs'
